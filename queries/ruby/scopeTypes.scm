(comment) @comment
(if) @ifStatement
(call) @functionCall
[(method) (singleton_method)] @namedFunction
(hash) @map

[
  (array)
  (string_array)
  (symbol_array)
] @list

(regex) @regularExpression

(class) @class
