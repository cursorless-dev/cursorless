{
  "name": "@cursorless/cursorless-talon-js",
  "version": "1.0.0",
  "description": "cursorless in talon js",
  "main": "./out/talon.js",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "esbuild:base": "esbuild ./src/extension.ts --conditions=cursorless:bundler --bundle --outfile=out/talon.js --external:talon --format=cjs --platform=neutral",
    "esbuild": "pnpm run esbuild:base --sourcemap",
    "esbuild:meta": "pnpm run esbuild:base --metafile=meta.json",
    "esbuild:watch": "pnpm run esbuild:base --sourcemap --watch",
    "esbuild:prod": "pnpm run esbuild:base --minify",
    "clean": "rm -rf ./out tsconfig.tsbuildinfo ./dist ./build"
  },
  "dependencies": {
    "@cursorless/common": "workspace:*",
    "@cursorless/cursorless-engine": "workspace:*",
    "lodash": "^4.17.21",
    "vscode-uri": "^3.0.8"
  },
  "devDependencies": {
    "@types/lodash": "4.17.0",
    "esbuild": "^0.20.2"
  }
}
