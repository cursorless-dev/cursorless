---
sidebar_label: Pair delimiter
---

import { ScopeVisualizer, ScopeVisualizerOptions, ScopeVisualizerProvider } from "@site/src/docs/components/ScopeVisualizer";
import scopeTests from "./fixtures/pairDelimiter.json";

# Pair delimiter

Cursorless ID: `pairDelimiter`

## Spoken form

Default: `pair delimiter`

## Syntax

- pair delimiter - Pair delimiter.

## Examples

- `"take pair delimiter blue air"` - Selects the pair delimiter containing the token containing letter `a` with a blue hat.

## Visualization

<ScopeVisualizerProvider scopeTests={scopeTests}>

The following are internal scopes. They are not intended for user interaction or spoken use. These scopes exist solely for internal Cursorless functionality.

<ScopeVisualizerOptions />

### Pair delimiter

<i>Internally used to add additional language specific surrounding pair delimiters in addition to the text based definitions. eg `r"`.</i>

##### JavaScript

<ScopeVisualizer fixtureName="scopes/javascript.core/pairDelimiter" />

##### Python

<ScopeVisualizer fixtureName="scopes/python/pairDelimiter" />

</ScopeVisualizerProvider>
