# Swap targets

Cursorless ID: `swapTargets`

## Spoken form

Default: `swap`

## Syntax

- swap with `<target>`: Swap selection with `<target>`.
- swap `<target 1>` with `<target 2>`: Swap `<target 1>` with `<target 2>`.
