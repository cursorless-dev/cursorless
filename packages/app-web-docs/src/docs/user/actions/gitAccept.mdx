# Git accept

Cursorless ID: `gitAccept`

## Spoken form

Default: `git accept`

## Syntax

- git accept `<target>`: Accept Git change at `<target>`.

## Examples

- `"git accept blue air"`: Accepts the Git change on the line containing the token containing letter 'a' with a blue hat.
