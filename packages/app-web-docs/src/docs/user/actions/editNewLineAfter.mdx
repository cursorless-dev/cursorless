# Edit new line/scope after

Scope defaults to line.

Cursorless ID: `editNewLineAfter`

## Spoken form

Default: `pour`

## Syntax

- pour `<target>`: Edit new line after `<target>`.
- pour `<scope>` `<target>`: Edit new `<scope>` after `<target>`.
