---
sidebar_label: Fold
---

# Fold (Fold region)

Cursorless ID: `foldRegion`

## Spoken form

Default: `fold`

## Syntax

- fold `<target>` - Fold region at `<target>`.

## Examples

- `"fold funk blue air"` - Folds the function containing the token containing letter `a` with a blue hat.
