---
sidebar_label: Git stage
---

# Git stage

Cursorless ID: `gitStage`

## Spoken form

Default: `git stage`

## Syntax

- git stage `<target>` - Stage Git change at `<target>`.

## Examples

- `"git stage blue air"` - Stages the Git change on the line containing the token containing letter 'a' with a blue hat.
