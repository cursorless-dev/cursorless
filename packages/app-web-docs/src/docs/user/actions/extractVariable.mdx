# Extract variable

Cursorless ID: `extractVariable`

## Spoken form

Default: `extract`

## Syntax

- extract `<target>`: Extract variable from `<target>`.

## Examples

- `"extract call blue air"`: Extracts the function call containing the token containing letter 'a' with a blue hat into a variable.
