---
sidebar_label: Rename
---

# Rename

Uses the editor's rename operation.

Cursorless ID: `rename`

## Spoken form

Default: `rename`

## Syntax

- rename `<target>` - Rename `<target>`.

## Examples

- `"rename blue air"` - Starts renaming the token containing letter 'a' with a blue hat.
