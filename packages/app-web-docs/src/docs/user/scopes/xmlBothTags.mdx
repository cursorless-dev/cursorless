import { Scopes } from "@site/src/docs/components/Scopes";

# XML both tags

Cursorless ID: `xmlBothTags`

## Spoken form

Default: `tags`

## Syntax

- tags: XML both tags.

## Examples

- `"take tags blue air"`: Selects the xml both tags containing the token containing letter 'a' with a blue hat.

<Scopes scopeTypeType="xmlBothTags" />
