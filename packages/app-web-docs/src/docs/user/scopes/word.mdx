import { Scopes } from "@site/src/docs/components/Scopes";

# Sub token word

Cursorless ID: `word`

## Spoken form

Default: `sub`\
Legacy: `word`

## Syntax

- sub: Sub token word.

## Examples

- `"take sub blue air"`: Selects the sub token word containing the token containing letter 'a' with a blue hat.

<Scopes scopeTypeType="word" />
