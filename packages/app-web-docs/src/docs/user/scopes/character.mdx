---
sidebar_label: Char
---

import { ScopeVisualizer, ScopeVisualizerOptions, ScopeVisualizerProvider } from "@site/src/docs/components/ScopeVisualizer";
import scopeTests from "./fixtures/character.json";

# Char (Character)

Cursorless ID: `character`

## Spoken form

Default: `char`

## Syntax

- char - Single character within a token.

## Examples

- `"take first char blue air"` - Selects the first character in the token containing letter `a` with a blue hat.

## Visualization

<ScopeVisualizerProvider scopeTests={scopeTests}>

<ScopeVisualizerOptions />

### Character

<i>A single character in the document</i>

##### Plaintext

<ScopeVisualizer fixtureName="scopes/plaintext/character" />

</ScopeVisualizerProvider>
