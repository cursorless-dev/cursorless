import { Scopes } from "@site/src/docs/components/Scopes";

# Document

Cursorless ID: `document`

## Spoken form

Default: `file`

## Syntax

- file: Document.

## Examples

- `"take file blue air"`: Selects the document containing the token containing letter 'a' with a blue hat.

<Scopes scopeTypeType="document" />
