import { Scopes } from "@site/src/docs/components/Scopes";

# XML element

Cursorless ID: `xmlElement`

## Spoken form

Default: `element`

## Syntax

- element: XML element.

## Examples

- `"take element blue air"`: Selects the XML element containing the token containing letter 'a' with a blue hat.

<Scopes scopeTypeType="xmlElement" />
