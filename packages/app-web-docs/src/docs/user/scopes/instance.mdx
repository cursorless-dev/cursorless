---
sidebar_label: Instance
---

import { Scopes } from "@site/src/docs/components/Scopes";

# Instance

Cursorless ID: `instance`

## Spoken form

Default: `instance`

## Syntax

- instance - Instance.

## Examples

- `"take every instance blue air"` - Selects every instance of the token containing letter 'a' with a blue hat.

<Scopes scopeTypeType="instance" />
