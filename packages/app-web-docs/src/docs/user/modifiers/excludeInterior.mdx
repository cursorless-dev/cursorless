---
sidebar_label: Bounds
---

import { ModifierVisualizer, ModifierVisualizerProvider, ModifierVisualizerOptions } from "@site/src/docs/components/ModifierVisualizer";

# Bounds (Exclude interior)

Cursorless ID: `excludeInterior`

## Spoken form

Default: `bounds`

## Syntax

- bounds - Bounding paired delimiters.

## Examples

- `"take bounds round blue air"` - Selects only the parentheses containing the token containing letter `a` with a blue hat.


<ModifierVisualizerProvider>

<ModifierVisualizerOptions />

<ModifierVisualizer fixtureName="modifiers/takeBounds" />

</ModifierVisualizerProvider>
