languageId: ruby
command:
  version: 1
  spokenForm: chuck every arg
  action: remove
  targets:
    - type: primitive
      modifier: {type: containingScope, scopeType: argumentOrParameter, includeSiblings: true}
initialState:
  documentContents: |-
    def hello_world(name, name2)
        puts "hi #{name} and #{name2}"
    end
  #             1         2         3
  #   0123456789012345678901234567890123
  #  +----------------------------------
  # 0|def hello_world(name, name2)
  # 1|    puts "hi #{name} and #{name2}"
  # 2|end
  selections:
    - anchor: {line: 0, character: 20}
      active: {line: 0, character: 20}
  marks: {}
finalState:
  documentContents: |-
    def hello_world()
        puts "hi #{name} and #{name2}"
    end
  #             1         2         3
  #   0123456789012345678901234567890123
  #  +----------------------------------
  # 0|def hello_world()
  # 1|    puts "hi #{name} and #{name2}"
  # 2|end
  selections:
    - anchor: {line: 0, character: 16}
      active: {line: 0, character: 16}
