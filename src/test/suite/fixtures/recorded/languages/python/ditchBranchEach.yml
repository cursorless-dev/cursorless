languageId: typescript
command:
  version: 3
  spokenForm: ditch branch each
  action: {name: remove}
  targets:
    - type: primitive
      modifiers:
        - type: containingScope
          scopeType: {type: branch}
      mark: {type: decoratedSymbol, symbolColor: default, character: e}
  usePrePhraseSnapshot: true
initialState:
  documentContents: |-
    if (true) {
      const whatever = "hello";
    } else if (false) {
      const whatever = "hello";
    } else {
      const whatever = "hello";
    }
  selections:
    - anchor: {line: 6, character: 1}
      active: {line: 6, character: 1}
  marks:
    default.e:
      start: {line: 2, character: 2}
      end: {line: 2, character: 6}
finalState:
  documentContents: |-
    if (true) {
      const whatever = "hello";
    }  else {
      const whatever = "hello";
    }
  selections:
    - anchor: {line: 4, character: 1}
      active: {line: 4, character: 1}
fullTargets: [{type: primitive, mark: {type: decoratedSymbol, symbolColor: default, character: e}, modifiers: [{type: containingScope, scopeType: {type: branch}}]}]
